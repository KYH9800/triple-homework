import React from 'react';
// styled-components
import { SectionWrapper } from '../styles/SectionSt';

const Section = (): any => {
  return (
    <SectionWrapper>
      <div>Welcome to Next.js!</div>
    </SectionWrapper>
  );
};

export default Section;
